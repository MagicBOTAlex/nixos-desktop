{
  vr.enable = true; # baller
  vscode.enable = true;
  wezterm.enable = true;

  discord = {
    limit = false;
    extended = false;
    allowedTime = 60;
  };

  blender.version = "5.0.0";

  kernel.useCachyOS = true;
  boot = {
    mineboot.enable = true;
  };
}
