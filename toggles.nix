{
  vr.enable = true; # baller
  vscode.enable = true;
  wezterm.enable = true;

  discord = {
    limit = true;
    extended = false;
    allowedTime = 180;
  };

  blender.version = "5.0.0";

  kernel.useCachyOS = true;
  boot = {
    mineboot.enable = true;
  };
}
