{
  vr.enable = true; # baller
  vscode.enable = true;
  wezterm.enable = true;

  discord = {
    limit = true;
    extended = false;
    allowedTime = 60;
  };

  kernel.useCachyOS = true;
  boot = {
    mineboot.enable = true;
  };
}
